"""Static seed data shipped with the application.

``books.json`` (Part 3) holds the sample catalogue used to seed the
vector store. Treat the file as read-only at runtime — mutating the
seed dataset is the job of the seeding script under ``scripts/``.
"""
