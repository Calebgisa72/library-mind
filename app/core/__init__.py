"""Core utilities: settings, logging, exceptions.

Modules here have no dependencies on the rest of ``app/`` (they sit at
the bottom of the dependency graph). Everything else may import from
``app.core`` freely.
"""
